# This is a special file that makes the folder a package.
# This file can be empty, but it must be present.
# Reason: it's an optional file that indicates to Python 
    # that the directory should be treated as a package.
# Not necessary in Python 3.3 and later, but still commonly used.